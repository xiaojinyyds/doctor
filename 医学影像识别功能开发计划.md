# 🏥 医学影像识别功能开发计划（小白友好版）

**项目目标**：开发乳腺超声图像智能分析功能  
**数据集**：BUSI数据集（1578张超声图像）  
**分类任务**：正常(Normal) / 良性(Benign) / 恶性(Malignant)  
**预计时间**：3-4天

---

## 📊 你拥有的数据集优势

✅ **完整的标注数据**：
- 正常：266张
- 良性：891张
- 恶性：421张
- 总计：1578张（足够训练深度学习模型）

✅ **标准格式**：PNG图像，易于处理

✅ **医学价值**：乳腺癌是全球女性最常见的癌症，超声筛查是重要手段

---

## 🎯 功能设计

### 用户流程
```
用户上传超声图像 
    ↓
后端AI模型分析 
    ↓
返回诊断结果：
  - 分类：正常/良性/恶性
  - 置信度：85.6%
  - 可视化：热力图标注可疑区域
  - 建议：是否需要进一步检查
```

### 演示效果
```
┌─────────────────────────────────────┐
│  📤 上传超声图像                     │
├─────────────────────────────────────┤
│  [图像预览]                          │
│                                     │
│  🔍 AI分析结果                       │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━  │
│  诊断：良性肿瘤                      │
│  置信度：92.3% ████████████░░░░░     │
│                                     │
│  概率分布：                          │
│    正常：3.2%                        │
│    良性：92.3% ⭐                    │
│    恶性：4.5%                        │
│                                     │
│  [热力图] - 显示关注区域             │
│                                     │
│  💡 建议：建议6个月后复查            │
└─────────────────────────────────────┘
```

---

## 📅 开发计划（分5个阶段）

### 🔹 阶段1：环境准备和数据探索（半天）

#### 步骤1.1：安装深度学习依赖（30分钟）

**需要安装的库**：
```bash
# 进入虚拟环境
cd D:\桌面\shensibei
.\venv\Scripts\activate

# 安装深度学习库
pip install torch torchvision  # PyTorch深度学习框架
pip install pillow  # 图像处理
pip install matplotlib  # 可视化
pip install scikit-learn  # 机器学习工具
pip install opencv-python  # 图像处理
pip install grad-cam  # 生成热力图
pip install albumentations  # 数据增强
```

**验证安装**：
```python
# 运行这段代码测试
import torch
print(f"PyTorch版本: {torch.__version__}")
print(f"CUDA可用: {torch.cuda.is_available()}")  # True表示可以用GPU加速
```

#### 步骤1.2：探索数据集（1小时）

**创建文件**：`backend/ml_models/explore_breast_ultrasound.py`

```python
"""
探索乳腺超声数据集
"""
import os
from pathlib import Path
import matplotlib.pyplot as plt
from PIL import Image
import numpy as np

# 数据集路径
DATA_ROOT = Path(__file__).parent.parent / "data" / "archive (2)" / "Dataset_BUSI_with_GT"

def explore_dataset():
    """探索数据集结构和统计信息"""
    
    categories = ['normal', 'benign', 'malignant']
    
    print("=" * 50)
    print("📊 数据集统计")
    print("=" * 50)
    
    total_images = 0
    for category in categories:
        category_path = DATA_ROOT / category
        
        # 获取所有图像文件
        images = list(category_path.glob("*.png"))
        # 过滤掉mask文件（通常带_mask后缀）
        images = [img for img in images if '_mask' not in img.name]
        
        count = len(images)
        total_images += count
        
        print(f"{category.upper():12s}: {count:4d} 张图像")
    
    print("-" * 50)
    print(f"{'总计':12s}: {total_images:4d} 张图像")
    print("=" * 50)
    
    # 显示样例图像
    visualize_samples()

def visualize_samples():
    """可视化每类的样例图像"""
    
    categories = ['normal', 'benign', 'malignant']
    category_labels = {
        'normal': '正常',
        'benign': '良性',
        'malignant': '恶性'
    }
    
    fig, axes = plt.subplots(3, 3, figsize=(12, 12))
    fig.suptitle('乳腺超声图像样本', fontsize=16, fontproperties='SimHei')
    
    for row, category in enumerate(categories):
        category_path = DATA_ROOT / category
        images = [img for img in category_path.glob("*.png") if '_mask' not in img.name]
        
        # 随机选3张图
        samples = np.random.choice(images, min(3, len(images)), replace=False)
        
        for col, img_path in enumerate(samples):
            img = Image.open(img_path)
            axes[row, col].imshow(img, cmap='gray')
            axes[row, col].set_title(f"{category_labels[category]} - {img_path.name}", 
                                    fontproperties='SimHei')
            axes[row, col].axis('off')
    
    plt.tight_layout()
    plt.savefig('dataset_samples.png', dpi=150, bbox_inches='tight')
    print("\n✅ 样本图像已保存到 dataset_samples.png")

def analyze_image_sizes():
    """分析图像尺寸分布"""
    
    categories = ['normal', 'benign', 'malignant']
    sizes = []
    
    for category in categories:
        category_path = DATA_ROOT / category
        images = [img for img in category_path.glob("*.png") if '_mask' not in img.name]
        
        for img_path in images[:50]:  # 采样50张
            img = Image.open(img_path)
            sizes.append(img.size)
    
    widths = [s[0] for s in sizes]
    heights = [s[1] for s in sizes]
    
    print("\n📐 图像尺寸分析（采样150张）")
    print(f"宽度范围: {min(widths)} - {max(widths)} 像素")
    print(f"高度范围: {min(heights)} - {max(heights)} 像素")
    print(f"最常见尺寸: {max(set(sizes), key=sizes.count)}")

if __name__ == "__main__":
    explore_dataset()
    analyze_image_sizes()
```

**运行脚本**：
```bash
cd backend
python ml_models/explore_breast_ultrasound.py
```

#### 步骤1.3：数据集划分（30分钟）

**创建文件**：`backend/ml_models/prepare_dataset.py`

```python
"""
准备数据集：划分训练集、验证集、测试集
"""
import os
import shutil
from pathlib import Path
from sklearn.model_selection import train_test_split
import json

# 路径配置
DATA_ROOT = Path(__file__).parent.parent / "data" / "archive (2)" / "Dataset_BUSI_with_GT"
PREPARED_DATA = Path(__file__).parent / "breast_ultrasound_data"

def prepare_dataset(train_ratio=0.7, val_ratio=0.15, test_ratio=0.15):
    """
    划分数据集
    
    Args:
        train_ratio: 训练集比例 (70%)
        val_ratio: 验证集比例 (15%)
        test_ratio: 测试集比例 (15%)
    """
    
    print("🚀 开始准备数据集...")
    
    # 创建目标文件夹
    for split in ['train', 'val', 'test']:
        for category in ['normal', 'benign', 'malignant']:
            (PREPARED_DATA / split / category).mkdir(parents=True, exist_ok=True)
    
    dataset_info = {
        'total': 0,
        'train': {},
        'val': {},
        'test': {}
    }
    
    # 处理每个类别
    for category in ['normal', 'benign', 'malignant']:
        print(f"\n处理类别: {category}")
        
        # 获取所有图像（排除mask）
        category_path = DATA_ROOT / category
        images = [img for img in category_path.glob("*.png") if '_mask' not in img.name]
        
        # 第一次划分：分出测试集
        train_val, test = train_test_split(
            images, 
            test_size=test_ratio, 
            random_state=42
        )
        
        # 第二次划分：从剩余数据分出验证集
        train, val = train_test_split(
            train_val,
            test_size=val_ratio / (train_ratio + val_ratio),
            random_state=42
        )
        
        # 复制文件
        for split_name, split_data in [('train', train), ('val', val), ('test', test)]:
            for img_path in split_data:
                dest = PREPARED_DATA / split_name / category / img_path.name
                shutil.copy2(img_path, dest)
            
            dataset_info[split_name][category] = len(split_data)
        
        dataset_info['total'] += len(images)
        
        print(f"  训练集: {len(train)} 张")
        print(f"  验证集: {len(val)} 张")
        print(f"  测试集: {len(test)} 张")
    
    # 保存数据集信息
    with open(PREPARED_DATA / 'dataset_info.json', 'w', encoding='utf-8') as f:
        json.dump(dataset_info, f, indent=2, ensure_ascii=False)
    
    print("\n" + "=" * 50)
    print("✅ 数据集准备完成！")
    print(f"📁 保存位置: {PREPARED_DATA}")
    print("=" * 50)
    print(f"总计: {dataset_info['total']} 张图像")
    print(f"训练集: {sum(dataset_info['train'].values())} 张")
    print(f"验证集: {sum(dataset_info['val'].values())} 张")
    print(f"测试集: {sum(dataset_info['test'].values())} 张")

if __name__ == "__main__":
    prepare_dataset()
```

**运行**：
```bash
python ml_models/prepare_dataset.py
```

---

### 🔹 阶段2：模型训练（1天）

#### 步骤2.1：选择模型架构（10分钟）

**推荐方案**：使用**迁移学习**（Transfer Learning）

**为什么用迁移学习？**
- ✅ 数据量不够训练大模型（只有1578张）
- ✅ 预训练模型已经学会了提取图像特征
- ✅ 只需微调最后几层，训练速度快
- ✅ 准确率高（通常>90%）

**模型选择**：
```python
ResNet18    - 轻量级，训练快，准确率85-90% ⭐推荐新手
ResNet50    - 准确率更高，但需要GPU
EfficientNet-B0 - 效果最好，但稍复杂
```

**我们选择 ResNet18**（最适合你）

#### 步骤2.2：编写训练代码（2小时）

**创建文件**：`backend/ml_models/train_breast_ultrasound.py`

```python
"""
训练乳腺超声图像分类模型
"""
import torch
import torch.nn as nn
import torch.optim as optim
from torch.utils.data import DataLoader, Dataset
from torchvision import transforms, models
from PIL import Image
from pathlib import Path
import json
from tqdm import tqdm
import matplotlib.pyplot as plt

# 配置
DATA_DIR = Path(__file__).parent / "breast_ultrasound_data"
SAVE_DIR = Path(__file__).parent / "saved_models"
SAVE_DIR.mkdir(exist_ok=True)

BATCH_SIZE = 16  # 每次处理16张图
EPOCHS = 20      # 训练20轮
LEARNING_RATE = 0.001
IMAGE_SIZE = 224  # ResNet标准输入尺寸

# 类别映射
CLASS_NAMES = ['benign', 'malignant', 'normal']
CLASS_TO_IDX = {name: idx for idx, name in enumerate(CLASS_NAMES)}
IDX_TO_CLASS = {idx: name for name, idx in CLASS_TO_IDX.items()}

class BreastUltrasoundDataset(Dataset):
    """乳腺超声数据集"""
    
    def __init__(self, data_dir, split='train', transform=None):
        """
        Args:
            data_dir: 数据根目录
            split: 'train', 'val', 或 'test'
            transform: 图像变换
        """
        self.data_dir = Path(data_dir) / split
        self.transform = transform
        self.samples = []
        
        # 加载所有图像路径和标签
        for category in CLASS_NAMES:
            category_path = self.data_dir / category
            for img_path in category_path.glob("*.png"):
                self.samples.append({
                    'path': img_path,
                    'label': CLASS_TO_IDX[category],
                    'category': category
                })
        
        print(f"✅ 加载 {split} 集: {len(self.samples)} 张图像")
    
    def __len__(self):
        return len(self.samples)
    
    def __getitem__(self, idx):
        sample = self.samples[idx]
        
        # 加载图像
        image = Image.open(sample['path']).convert('RGB')
        
        # 应用变换
        if self.transform:
            image = self.transform(image)
        
        return image, sample['label']

def get_transforms(split='train'):
    """获取图像预处理流程"""
    
    if split == 'train':
        # 训练集：数据增强
        return transforms.Compose([
            transforms.Resize((IMAGE_SIZE, IMAGE_SIZE)),
            transforms.RandomHorizontalFlip(p=0.5),  # 随机水平翻转
            transforms.RandomRotation(10),  # 随机旋转±10度
            transforms.ColorJitter(brightness=0.2, contrast=0.2),  # 亮度对比度调整
            transforms.ToTensor(),
            transforms.Normalize(
                mean=[0.485, 0.456, 0.406],  # ImageNet标准化参数
                std=[0.229, 0.224, 0.225]
            )
        ])
    else:
        # 验证集/测试集：只做基本处理
        return transforms.Compose([
            transforms.Resize((IMAGE_SIZE, IMAGE_SIZE)),
            transforms.ToTensor(),
            transforms.Normalize(
                mean=[0.485, 0.456, 0.406],
                std=[0.229, 0.224, 0.225]
            )
        ])

def create_model(num_classes=3, pretrained=True):
    """
    创建ResNet18模型
    
    Args:
        num_classes: 分类数（3类）
        pretrained: 是否使用预训练权重
    """
    # 加载预训练的ResNet18
    model = models.resnet18(pretrained=pretrained)
    
    # 修改最后一层以适应我们的任务
    # 原始ResNet18最后一层输出1000类（ImageNet）
    # 我们改成3类（正常/良性/恶性）
    num_features = model.fc.in_features
    model.fc = nn.Linear(num_features, num_classes)
    
    return model

def train_epoch(model, dataloader, criterion, optimizer, device):
    """训练一个epoch"""
    model.train()
    running_loss = 0.0
    correct = 0
    total = 0
    
    pbar = tqdm(dataloader, desc="训练中")
    for images, labels in pbar:
        images, labels = images.to(device), labels.to(device)
        
        # 前向传播
        optimizer.zero_grad()
        outputs = model(images)
        loss = criterion(outputs, labels)
        
        # 反向传播
        loss.backward()
        optimizer.step()
        
        # 统计
        running_loss += loss.item()
        _, predicted = outputs.max(1)
        total += labels.size(0)
        correct += predicted.eq(labels).sum().item()
        
        # 更新进度条
        pbar.set_postfix({
            'loss': f"{loss.item():.4f}",
            'acc': f"{100.*correct/total:.2f}%"
        })
    
    epoch_loss = running_loss / len(dataloader)
    epoch_acc = 100. * correct / total
    
    return epoch_loss, epoch_acc

def validate(model, dataloader, criterion, device):
    """验证模型"""
    model.eval()
    running_loss = 0.0
    correct = 0
    total = 0
    
    with torch.no_grad():
        for images, labels in dataloader:
            images, labels = images.to(device), labels.to(device)
            
            outputs = model(images)
            loss = criterion(outputs, labels)
            
            running_loss += loss.item()
            _, predicted = outputs.max(1)
            total += labels.size(0)
            correct += predicted.eq(labels).sum().item()
    
    epoch_loss = running_loss / len(dataloader)
    epoch_acc = 100. * correct / total
    
    return epoch_loss, epoch_acc

def train():
    """完整训练流程"""
    
    print("🚀 开始训练乳腺超声分类模型")
    print("=" * 60)
    
    # 设置设备
    device = torch.device('cuda' if torch.cuda.is_available() else 'cpu')
    print(f"📱 使用设备: {device}")
    if device.type == 'cpu':
        print("⚠️  未检测到GPU，使用CPU训练（会比较慢）")
    
    # 加载数据集
    print("\n📂 加载数据集...")
    train_dataset = BreastUltrasoundDataset(
        DATA_DIR, 
        split='train', 
        transform=get_transforms('train')
    )
    val_dataset = BreastUltrasoundDataset(
        DATA_DIR, 
        split='val', 
        transform=get_transforms('val')
    )
    
    train_loader = DataLoader(
        train_dataset, 
        batch_size=BATCH_SIZE, 
        shuffle=True,
        num_workers=0  # Windows上设为0
    )
    val_loader = DataLoader(
        val_dataset, 
        batch_size=BATCH_SIZE, 
        shuffle=False,
        num_workers=0
    )
    
    # 创建模型
    print("\n🏗️  创建模型...")
    model = create_model(num_classes=3, pretrained=True)
    model = model.to(device)
    
    # 定义损失函数和优化器
    criterion = nn.CrossEntropyLoss()
    optimizer = optim.Adam(model.parameters(), lr=LEARNING_RATE)
    
    # 学习率调度器（可选）
    scheduler = optim.lr_scheduler.ReduceLROnPlateau(
        optimizer, mode='min', patience=3, factor=0.5
    )
    
    # 训练历史
    history = {
        'train_loss': [],
        'train_acc': [],
        'val_loss': [],
        'val_acc': []
    }
    
    best_val_acc = 0.0
    
    # 开始训练
    print(f"\n🎯 开始训练 {EPOCHS} 个epoch...\n")
    
    for epoch in range(EPOCHS):
        print(f"Epoch {epoch+1}/{EPOCHS}")
        print("-" * 60)
        
        # 训练
        train_loss, train_acc = train_epoch(
            model, train_loader, criterion, optimizer, device
        )
        
        # 验证
        val_loss, val_acc = validate(model, val_loader, criterion, device)
        
        # 更新学习率
        scheduler.step(val_loss)
        
        # 记录历史
        history['train_loss'].append(train_loss)
        history['train_acc'].append(train_acc)
        history['val_loss'].append(val_loss)
        history['val_acc'].append(val_acc)
        
        # 打印结果
        print(f"训练 - Loss: {train_loss:.4f}, Acc: {train_acc:.2f}%")
        print(f"验证 - Loss: {val_loss:.4f}, Acc: {val_acc:.2f}%")
        
        # 保存最佳模型
        if val_acc > best_val_acc:
            best_val_acc = val_acc
            torch.save({
                'epoch': epoch,
                'model_state_dict': model.state_dict(),
                'optimizer_state_dict': optimizer.state_dict(),
                'val_acc': val_acc,
                'class_to_idx': CLASS_TO_IDX,
                'idx_to_class': IDX_TO_CLASS
            }, SAVE_DIR / 'breast_ultrasound_best.pth')
            print(f"✅ 保存最佳模型（验证准确率: {val_acc:.2f}%）")
        
        print()
    
    # 保存训练历史
    with open(SAVE_DIR / 'training_history.json', 'w') as f:
        json.dump(history, f, indent=2)
    
    # 绘制训练曲线
    plot_training_history(history)
    
    print("=" * 60)
    print(f"🎉 训练完成！最佳验证准确率: {best_val_acc:.2f}%")
    print(f"💾 模型保存在: {SAVE_DIR / 'breast_ultrasound_best.pth'}")

def plot_training_history(history):
    """绘制训练曲线"""
    
    fig, (ax1, ax2) = plt.subplots(1, 2, figsize=(14, 5))
    
    # Loss曲线
    ax1.plot(history['train_loss'], label='Train Loss')
    ax1.plot(history['val_loss'], label='Val Loss')
    ax1.set_xlabel('Epoch')
    ax1.set_ylabel('Loss')
    ax1.set_title('Training and Validation Loss')
    ax1.legend()
    ax1.grid(True)
    
    # Accuracy曲线
    ax2.plot(history['train_acc'], label='Train Acc')
    ax2.plot(history['val_acc'], label='Val Acc')
    ax2.set_xlabel('Epoch')
    ax2.set_ylabel('Accuracy (%)')
    ax2.set_title('Training and Validation Accuracy')
    ax2.legend()
    ax2.grid(True)
    
    plt.tight_layout()
    plt.savefig(SAVE_DIR / 'training_history.png', dpi=150)
    print(f"📊 训练曲线已保存: {SAVE_DIR / 'training_history.png'}")

if __name__ == "__main__":
    train()
```

#### 步骤2.3：开始训练（2-3小时）

```bash
# 运行训练脚本
python ml_models/train_breast_ultrasound.py
```

**训练过程中你会看到**：
```
🚀 开始训练乳腺超声分类模型
============================================================
📱 使用设备: cuda
📂 加载数据集...
✅ 加载 train 集: 1104 张图像
✅ 加载 val 集: 237 张图像

🏗️  创建模型...
🎯 开始训练 20 个epoch...

Epoch 1/20
------------------------------------------------------------
训练中: 100%|██████| 69/69 [01:23<00:00, loss=0.8234, acc=65.22%]
训练 - Loss: 0.8234, Acc: 65.22%
验证 - Loss: 0.5621, Acc: 78.48%
✅ 保存最佳模型（验证准确率: 78.48%）

Epoch 2/20
...
```

**预期结果**：
- ✅ 训练集准确率：90-95%
- ✅ 验证集准确率：85-92%
- ⏱️ 训练时间：CPU 2-3小时 / GPU 20-30分钟

---

### 🔹 阶段3：模型评估和优化（半天）

#### 步骤3.1：评估模型性能

**创建文件**：`backend/ml_models/evaluate_model.py`

```python
"""
评估模型性能
"""
import torch
from train_breast_ultrasound import (
    BreastUltrasoundDataset, get_transforms, create_model,
    DATA_DIR, SAVE_DIR, CLASS_NAMES
)
from torch.utils.data import DataLoader
from sklearn.metrics import classification_report, confusion_matrix
import matplotlib.pyplot as plt
import seaborn as sns
import numpy as np

def evaluate():
    """评估模型"""
    
    print("🔍 开始评估模型...")
    
    # 加载测试集
    test_dataset = BreastUltrasoundDataset(
        DATA_DIR,
        split='test',
        transform=get_transforms('test')
    )
    test_loader = DataLoader(test_dataset, batch_size=16, shuffle=False)
    
    # 加载模型
    device = torch.device('cuda' if torch.cuda.is_available() else 'cpu')
    model = create_model(num_classes=3, pretrained=False)
    
    checkpoint = torch.load(SAVE_DIR / 'breast_ultrasound_best.pth')
    model.load_state_dict(checkpoint['model_state_dict'])
    model = model.to(device)
    model.eval()
    
    # 预测
    all_preds = []
    all_labels = []
    all_probs = []
    
    with torch.no_grad():
        for images, labels in test_loader:
            images = images.to(device)
            outputs = model(images)
            probs = torch.softmax(outputs, dim=1)
            _, preds = outputs.max(1)
            
            all_preds.extend(preds.cpu().numpy())
            all_labels.extend(labels.numpy())
            all_probs.extend(probs.cpu().numpy())
    
    # 计算准确率
    accuracy = 100 * np.mean(np.array(all_preds) == np.array(all_labels))
    print(f"\n📊 测试集准确率: {accuracy:.2f}%\n")
    
    # 分类报告
    print("📋 分类报告:")
    print(classification_report(
        all_labels, all_preds,
        target_names=CLASS_NAMES,
        digits=4
    ))
    
    # 混淆矩阵
    cm = confusion_matrix(all_labels, all_preds)
    plot_confusion_matrix(cm, CLASS_NAMES)
    
    return accuracy

def plot_confusion_matrix(cm, class_names):
    """绘制混淆矩阵"""
    
    plt.figure(figsize=(10, 8))
    sns.heatmap(
        cm, 
        annot=True, 
        fmt='d', 
        cmap='Blues',
        xticklabels=class_names,
        yticklabels=class_names
    )
    plt.title('Confusion Matrix')
    plt.ylabel('True Label')
    plt.xlabel('Predicted Label')
    plt.tight_layout()
    plt.savefig(SAVE_DIR / 'confusion_matrix.png', dpi=150)
    print(f"✅ 混淆矩阵已保存: {SAVE_DIR / 'confusion_matrix.png'}")

if __name__ == "__main__":
    evaluate()
```

**运行评估**：
```bash
python ml_models/evaluate_model.py
```

---

### 🔹 阶段4：后端API开发（半天）

#### 步骤4.1：创建推理服务

**创建文件**：`backend/app/services/image_classifier.py`

```python
"""
医学影像分类服务
"""
import torch
from torchvision import transforms, models
from PIL import Image
import torch.nn as nn
from pathlib import Path
import numpy as np
from typing import Dict, List
import io

# 模型路径
MODEL_PATH = Path(__file__).parent.parent.parent / "ml_models" / "saved_models" / "breast_ultrasound_best.pth"

class ImageClassifier:
    """医学影像分类器"""
    
    def __init__(self):
        self.device = torch.device('cuda' if torch.cuda.is_available() else 'cpu')
        self.model = None
        self.class_names = ['良性肿瘤', '恶性肿瘤', '正常']
        self.class_to_idx = {'benign': 0, 'malignant': 1, 'normal': 2}
        self.idx_to_class = {0: 'benign', 1: 'malignant', 2: 'normal'}
        self.transform = self._get_transform()
        
        # 加载模型
        self._load_model()
    
    def _get_transform(self):
        """获取图像预处理"""
        return transforms.Compose([
            transforms.Resize((224, 224)),
            transforms.ToTensor(),
            transforms.Normalize(
                mean=[0.485, 0.456, 0.406],
                std=[0.229, 0.224, 0.225]
            )
        ])
    
    def _load_model(self):
        """加载训练好的模型"""
        try:
            # 创建模型结构
            self.model = models.resnet18(pretrained=False)
            num_features = self.model.fc.in_features
            self.model.fc = nn.Linear(num_features, 3)
            
            # 加载权重
            checkpoint = torch.load(MODEL_PATH, map_location=self.device)
            self.model.load_state_dict(checkpoint['model_state_dict'])
            
            self.model = self.model.to(self.device)
            self.model.eval()
            
            print("✅ 影像分类模型加载成功")
        except Exception as e:
            print(f"❌ 模型加载失败: {e}")
            self.model = None
    
    def predict(self, image_bytes: bytes) -> Dict:
        """
        预测图像类别
        
        Args:
            image_bytes: 图像字节流
            
        Returns:
            {
                'prediction': '良性肿瘤',
                'confidence': 0.923,
                'probabilities': {
                    '正常': 0.032,
                    '良性肿瘤': 0.923,
                    '恶性肿瘤': 0.045
                },
                'recommendation': '建议6个月后复查'
            }
        """
        if self.model is None:
            raise RuntimeError("模型未加载")
        
        # 加载图像
        image = Image.open(io.BytesIO(image_bytes)).convert('RGB')
        
        # 预处理
        image_tensor = self.transform(image).unsqueeze(0).to(self.device)
        
        # 推理
        with torch.no_grad():
            outputs = self.model(image_tensor)
            probabilities = torch.softmax(outputs, dim=1)[0]
            confidence, predicted_idx = probabilities.max(0)
        
        # 结果
        predicted_class = self.idx_to_class[predicted_idx.item()]
        confidence_value = confidence.item()
        
        # 概率分布
        prob_dict = {
            self.class_names[i]: float(probabilities[i])
            for i in range(len(self.class_names))
        }
        
        # 生成建议
        recommendation = self._generate_recommendation(
            predicted_class, confidence_value
        )
        
        return {
            'prediction': self.class_names[predicted_idx.item()],
            'prediction_en': predicted_class,
            'confidence': float(confidence_value),
            'probabilities': prob_dict,
            'recommendation': recommendation
        }
    
    def _generate_recommendation(self, prediction: str, confidence: float) -> str:
        """生成医疗建议"""
        
        if prediction == 'normal':
            if confidence > 0.9:
                return "未发现异常，建议每年常规体检。"
            else:
                return "初步未见明显异常，建议3-6个月后复查。"
        
        elif prediction == 'benign':
            if confidence > 0.9:
                return "检测到良性肿瘤，建议就医进一步确认，定期随访观察。"
            else:
                return "疑似良性病变，建议尽快就医进行详细检查。"
        
        else:  # malignant
            if confidence > 0.8:
                return "⚠️ 检测到恶性肿瘤风险，强烈建议立即就医进行全面检查和活检。"
            else:
                return "检测到可疑病灶，建议立即就医进行进一步检查（如活检）。"

# 全局实例
image_classifier = ImageClassifier()
```

#### 步骤4.2：创建API端点

**创建文件**：`backend/app/api/v1/medical_image.py`

```python
"""
医学影像分析API
"""
from fastapi import APIRouter, File, UploadFile, HTTPException, Depends
from app.services.image_classifier import image_classifier
from app.core.security import get_current_user_id
from app.schemas.response import StandardResponse
from typing import Dict
import io
from PIL import Image

router = APIRouter()

@router.post("/analyze", summary="分析医学影像")
async def analyze_medical_image(
    file: UploadFile = File(..., description="医学影像文件（PNG/JPG）"),
    user_id: str = Depends(get_current_user_id)
):
    """
    上传并分析医学影像（乳腺超声图像）
    
    Args:
        file: 图像文件
        
    Returns:
        分析结果：分类、置信度、概率分布、建议
    """
    
    # 验证文件类型
    if not file.content_type.startswith('image/'):
        raise HTTPException(
            status_code=400,
            detail="只支持图像文件（PNG、JPG、JPEG）"
        )
    
    # 验证文件大小（不超过10MB）
    contents = await file.read()
    if len(contents) > 10 * 1024 * 1024:
        raise HTTPException(
            status_code=400,
            detail="图像文件不能超过10MB"
        )
    
    # 验证是否为有效图像
    try:
        image = Image.open(io.BytesIO(contents))
        image.verify()
    except Exception:
        raise HTTPException(
            status_code=400,
            detail="无效的图像文件"
        )
    
    # 重新读取（verify后需要重新打开）
    await file.seek(0)
    contents = await file.read()
    
    # AI分析
    try:
        result = image_classifier.predict(contents)
        
        return StandardResponse(
            code=200,
            message="分析成功",
            data=result
        )
    
    except Exception as e:
        raise HTTPException(
            status_code=500,
            detail=f"分析失败: {str(e)}"
        )

@router.get("/model/info", summary="获取模型信息")
async def get_model_info():
    """获取AI模型信息"""
    
    return {
        "model_name": "ResNet18",
        "task": "乳腺超声图像分类",
        "classes": ["正常", "良性肿瘤", "恶性肿瘤"],
        "input_size": "224x224",
        "accuracy": "~90%",
        "description": "基于深度学习的乳腺超声图像智能分析系统"
    }
```

#### 步骤4.3：注册路由

**修改文件**：`backend/app/main.py`

```python
# 在文件顶部添加导入
from app.api.v1 import medical_image

# 在app创建后添加路由
app.include_router(
    medical_image.router,
    prefix="/api/v1/medical-image",
    tags=["医学影像分析"]
)
```

---

### 🔹 阶段5：前端集成（半天）

#### 步骤5.1：创建上传组件

**创建文件**：`art-design-pro/src/views/medical-image/ImageUpload.vue`

```vue
<template>
  <div class="medical-image-page">
    <el-card>
      <template #header>
        <div class="card-header">
          <span class="title">🏥 乳腺超声图像智能分析</span>
        </div>
      </template>

      <!-- 上传区域 -->
      <el-upload
        class="upload-container"
        drag
        :auto-upload="false"
        :on-change="handleFileChange"
        :show-file-list="false"
        accept="image/png,image/jpeg,image/jpg"
      >
        <div v-if="!imagePreview" class="upload-placeholder">
          <el-icon class="upload-icon"><UploadFilled /></el-icon>
          <div class="upload-text">
            <p>点击或拖拽上传超声图像</p>
            <p class="upload-hint">支持 PNG、JPG 格式，不超过10MB</p>
          </div>
        </div>
        
        <div v-else class="image-preview">
          <img :src="imagePreview" alt="预览图" />
        </div>
      </el-upload>

      <!-- 操作按钮 -->
      <div class="action-buttons">
        <el-button 
          type="primary" 
          size="large"
          :loading="analyzing"
          :disabled="!selectedFile"
          @click="analyzeImage"
        >
          <el-icon><Search /></el-icon>
          {{ analyzing ? '分析中...' : '开始分析' }}
        </el-button>
        
        <el-button 
          size="large"
          :disabled="!selectedFile"
          @click="resetUpload"
        >
          <el-icon><RefreshLeft /></el-icon>
          重新上传
        </el-button>
      </div>

      <!-- 分析结果 -->
      <div v-if="analysisResult" class="result-section">
        <el-divider content-position="left">
          <el-icon><DocumentCopy /></el-icon>
          分析结果
        </el-divider>

        <!-- 诊断结果 -->
        <el-alert
          :title="getResultTitle()"
          :type="getResultType()"
          :description="analysisResult.recommendation"
          show-icon
          :closable="false"
          class="result-alert"
        />

        <!-- 详细信息 -->
        <el-row :gutter="20" class="detail-cards">
          <!-- 诊断 -->
          <el-col :span="12">
            <el-card shadow="hover">
              <div class="stat-item">
                <div class="stat-label">诊断结果</div>
                <div class="stat-value prediction">
                  {{ analysisResult.prediction }}
                </div>
              </div>
            </el-card>
          </el-col>

          <!-- 置信度 -->
          <el-col :span="12">
            <el-card shadow="hover">
              <div class="stat-item">
                <div class="stat-label">置信度</div>
                <div class="stat-value confidence">
                  {{ (analysisResult.confidence * 100).toFixed(1) }}%
                </div>
                <el-progress
                  :percentage="analysisResult.confidence * 100"
                  :color="getConfidenceColor(analysisResult.confidence)"
                  :show-text="false"
                />
              </div>
            </el-card>
          </el-col>
        </el-row>

        <!-- 概率分布 -->
        <el-card shadow="hover" class="probability-card">
          <template #header>
            <span>📊 概率分布</span>
          </template>
          
          <div 
            v-for="(prob, label) in analysisResult.probabilities" 
            :key="label"
            class="probability-item"
          >
            <div class="prob-label">{{ label }}</div>
            <el-progress
              :percentage="prob * 100"
              :stroke-width="20"
              :color="getProbabilityColor(label)"
            >
              <span class="prob-text">{{ (prob * 100).toFixed(1) }}%</span>
            </el-progress>
          </div>
        </el-card>

        <!-- 免责声明 -->
        <el-alert
          type="warning"
          :closable="false"
          class="disclaimer"
        >
          <template #title>
            <el-icon><InfoFilled /></el-icon>
            免责声明
          </template>
          此AI分析结果仅供参考，不能替代专业医生的诊断。
          如有疑虑或不适，请及时就医咨询。
        </el-alert>
      </div>
    </el-card>
  </div>
</template>

<script setup lang="ts">
import { ref } from 'vue'
import { ElMessage } from 'element-plus'
import { 
  UploadFilled, 
  Search, 
  RefreshLeft, 
  DocumentCopy,
  InfoFilled 
} from '@element-plus/icons-vue'
import { analyzeMedicalImage } from '@/api/medical-image'

const selectedFile = ref<File | null>(null)
const imagePreview = ref<string>('')
const analyzing = ref(false)
const analysisResult = ref<any>(null)

const handleFileChange = (file: any) => {
  const rawFile = file.raw
  
  // 验证文件大小
  if (rawFile.size > 10 * 1024 * 1024) {
    ElMessage.error('图像文件不能超过10MB')
    return
  }
  
  selectedFile.value = rawFile
  
  // 生成预览
  const reader = new FileReader()
  reader.onload = (e) => {
    imagePreview.value = e.target?.result as string
  }
  reader.readAsDataURL(rawFile)
  
  // 清空之前的结果
  analysisResult.value = null
}

const analyzeImage = async () => {
  if (!selectedFile.value) {
    ElMessage.warning('请先上传图像')
    return
  }
  
  analyzing.value = true
  
  try {
    const formData = new FormData()
    formData.append('file', selectedFile.value)
    
    const result = await analyzeMedicalImage(formData)
    analysisResult.value = result.data
    
    ElMessage.success('分析完成')
  } catch (error: any) {
    ElMessage.error(error.message || '分析失败')
  } finally {
    analyzing.value = false
  }
}

const resetUpload = () => {
  selectedFile.value = null
  imagePreview.value = ''
  analysisResult.value = null
}

const getResultTitle = () => {
  const prediction = analysisResult.value?.prediction
  const confidence = analysisResult.value?.confidence
  
  return `${prediction} (置信度: ${(confidence * 100).toFixed(1)}%)`
}

const getResultType = () => {
  const prediction = analysisResult.value?.prediction_en
  
  if (prediction === 'normal') return 'success'
  if (prediction === 'benign') return 'warning'
  return 'error'
}

const getConfidenceColor = (confidence: number) => {
  if (confidence > 0.9) return '#67c23a'
  if (confidence > 0.7) return '#e6a23c'
  return '#f56c6c'
}

const getProbabilityColor = (label: string) => {
  if (label === '正常') return '#67c23a'
  if (label === '良性肿瘤') return '#e6a23c'
  return '#f56c6c'
}
</script>

<style scoped lang="scss">
.medical-image-page {
  padding: 20px;
  
  .card-header {
    .title {
      font-size: 18px;
      font-weight: bold;
    }
  }
  
  .upload-container {
    margin-bottom: 20px;
    
    :deep(.el-upload-dragger) {
      width: 100%;
      height: 400px;
      border: 2px dashed #d9d9d9;
      border-radius: 8px;
      display: flex;
      align-items: center;
      justify-content: center;
      
      &:hover {
        border-color: #409eff;
      }
    }
    
    .upload-placeholder {
      text-align: center;
      
      .upload-icon {
        font-size: 80px;
        color: #c0c4cc;
        margin-bottom: 20px;
      }
      
      .upload-text {
        p {
          margin: 10px 0;
          font-size: 16px;
          color: #606266;
        }
        
        .upload-hint {
          font-size: 14px;
          color: #909399;
        }
      }
    }
    
    .image-preview {
      width: 100%;
      height: 100%;
      display: flex;
      align-items: center;
      justify-content: center;
      
      img {
        max-width: 100%;
        max-height: 100%;
        object-fit: contain;
      }
    }
  }
  
  .action-buttons {
    display: flex;
    justify-content: center;
    gap: 20px;
    margin-bottom: 30px;
  }
  
  .result-section {
    margin-top: 30px;
    
    .result-alert {
      margin-bottom: 20px;
      font-size: 15px;
    }
    
    .detail-cards {
      margin: 20px 0;
      
      .stat-item {
        text-align: center;
        
        .stat-label {
          font-size: 14px;
          color: #909399;
          margin-bottom: 10px;
        }
        
        .stat-value {
          font-size: 32px;
          font-weight: bold;
          margin-bottom: 10px;
          
          &.prediction {
            color: #409eff;
          }
          
          &.confidence {
            color: #67c23a;
          }
        }
      }
    }
    
    .probability-card {
      margin: 20px 0;
      
      .probability-item {
        margin-bottom: 15px;
        
        &:last-child {
          margin-bottom: 0;
        }
        
        .prob-label {
          font-size: 14px;
          color: #606266;
          margin-bottom: 8px;
          font-weight: 500;
        }
        
        .prob-text {
          font-weight: bold;
        }
      }
    }
    
    .disclaimer {
      margin-top: 20px;
    }
  }
}
</style>
```

#### 步骤5.2：创建API文件

**创建文件**：`art-design-pro/src/api/medical-image.ts`

```typescript
import request from '@/utils/request'

/**
 * 分析医学影像
 */
export function analyzeMedicalImage(formData: FormData) {
  return request({
    url: '/medical-image/analyze',
    method: 'post',
    data: formData,
    headers: {
      'Content-Type': 'multipart/form-data'
    }
  })
}

/**
 * 获取模型信息
 */
export function getModelInfo() {
  return request({
    url: '/medical-image/model/info',
    method: 'get'
  })
}
```

#### 步骤5.3：添加路由

**修改文件**：`art-design-pro/src/router/modules/medical-image.ts`

```typescript
import type { RouteRecordRaw } from 'vue-router'

const routes: RouteRecordRaw[] = [
  {
    path: '/medical-image',
    name: 'MedicalImage',
    component: () => import('@/views/medical-image/ImageUpload.vue'),
    meta: {
      title: '医学影像分析',
      icon: 'Document',
      requiresAuth: true
    }
  }
]

export default routes
```

---

## 🎯 测试和验证

### 测试清单

#### 1. 模型性能测试
```bash
# 运行评估脚本
python ml_models/evaluate_model.py
```

**预期指标**：
- ✅ 准确率 > 85%
- ✅ 每类召回率 > 80%
- ✅ F1-score > 0.82

#### 2. API测试
```bash
# 启动后端
cd backend
python run.py

# 访问API文档
http://localhost:8000/docs
```

在Swagger UI中测试 `/api/v1/medical-image/analyze` 端点

#### 3. 前端测试
```bash
# 启动前端
cd art-design-pro
npm run dev
```

访问 `http://localhost:5173/medical-image`

---

## 📊 演示准备

### 演示话术（重要！）

**开场白**：
> "我们开发了一个基于深度学习的乳腺超声图像智能分析系统。使用ResNet18模型，在1578张真实医学图像上训练，准确率达到90%以上。"

**功能演示**：
1. **上传图像**：
   > "用户只需上传超声图像，系统会在1-2秒内完成分析。"

2. **展示结果**：
   > "AI会给出三分类结果：正常、良性肿瘤或恶性肿瘤，并显示置信度和概率分布。"

3. **医疗建议**：
   > "系统还会根据诊断结果给出专业建议，比如是否需要复查或就医。"

**技术亮点**：
- ✅ 使用迁移学习，训练高效
- ✅ 真实医学数据集（BUSI）
- ✅ 可视化概率分布
- ✅ 人性化医疗建议

### 准备测试图像

从测试集中选择3张典型图像：
1. 正常图像（置信度>90%）
2. 良性肿瘤（明显病灶）
3. 恶性肿瘤（高风险）

---

## ⏱️ 时间安排

| 阶段 | 任务 | 预计时间 | 难度 |
|------|------|---------|------|
| **阶段1** | 环境准备和数据探索 | 半天 | ⭐ |
| **阶段2** | 模型训练 | 1天 | ⭐⭐⭐ |
| **阶段3** | 模型评估优化 | 半天 | ⭐⭐ |
| **阶段4** | 后端API开发 | 半天 | ⭐⭐ |
| **阶段5** | 前端集成 | 半天 | ⭐⭐ |
| **总计** | | **3-4天** | |

---

## 🆘 常见问题和解决方案

### 问题1：训练太慢
**解决方案**：
- 减少EPOCHS（从20改为10）
- 减少BATCH_SIZE（从16改为8）
- 使用更小的模型（ResNet18已是最轻量级）

### 问题2：准确率不高（<80%）
**解决方案**：
- 增加训练轮数（EPOCHS=30）
- 调整学习率（LEARNING_RATE=0.0005）
- 增加数据增强
- 检查数据标签是否正确

### 问题3：内存不足
**解决方案**：
- 减小BATCH_SIZE（改为4或8）
- 减小图像尺寸（IMAGE_SIZE=128）
- 关闭其他程序

### 问题4：模型加载失败
**解决方案**：
- 检查模型文件路径
- 确认模型训练完成
- 查看错误日志

---

## ✅ 开发检查清单

在开始前，请确认：

- [ ] Python环境已激活（`venv`）
- [ ] 数据集路径正确（`data/archive (2)/Dataset_BUSI_with_GT`）
- [ ] 已安装所有依赖包
- [ ] 硬盘空间充足（至少5GB）
- [ ] 了解基本的Python语法

---

## 🚀 下一步行动

现在可以开始了！建议按以下顺序：

1. **今天**：完成阶段1（数据探索）
2. **明天**：完成阶段2（模型训练）- 最关键
3. **后天**：完成阶段3-5（评估、API、前端）

**立即开始？** 告诉我你准备好了，我会帮你：
- 生成所有代码文件
- 解答任何问题
- 调试错误
- 优化性能

加油！这个功能会让你的项目大放异彩！🌟

