好的！我先阅读您的已有代码，然后制定详细的开发计划。



# 📋 三个功能开发计划

基于您现有的代码结构，我为您制定了详细的开发计划。

---

## 📊 现有代码分析

### ✅ 已有资源
- **Models**: [Assessment](cci:2://file:///d:/%E6%A1%8C%E9%9D%A2/%E7%A5%9E%E6%80%9D%E6%9D%AF/backend/app/models/assessment.py:7:0-51:9), [Questionnaire](cci:2://file:///d:/%E6%A1%8C%E9%9D%A2/%E7%A5%9E%E6%80%9D%E6%9D%AF/backend/app/models/questionnaire.py:7:0-70:9), [Report](cci:2://file:///d:/%E6%A1%8C%E9%9D%A2/%E7%A5%9E%E6%80%9D%E6%9D%AF/backend/app/models/assessment.py:81:0-112:9), [User](cci:2://file:///d:/%E6%A1%8C%E9%9D%A2/%E7%A5%9E%E6%80%9D%E6%9D%AF/backend/app/models/user.py:21:0-49:9), [Recommendation](cci:2://file:///d:/%E6%A1%8C%E9%9D%A2/%E7%A5%9E%E6%80%9D%E6%9D%AF/backend/app/models/assessment.py:54:0-78:9)
- **现有API**: 
  - `/assessment/history` - 历史列表
  - `/assessment/record/{id}` - 单条详情
  - `/admin/users` - 用户管理
  - `/admin/statistics/overview` - 基础统计
- **认证**: [get_current_user_id](cci:1://file:///d:/%E6%A1%8C%E9%9D%A2/%E7%A5%9E%E6%80%9D%E6%9D%AF/backend/app/core/security.py:67:0-81:25), [require_admin](cci:1://file:///d:/%E6%A1%8C%E9%9D%A2/%E7%A5%9E%E6%80%9D%E6%9D%AF/backend/app/core/security.py:84:0-107:25)
- **数据库**: Report模型已有 `share_token`, `share_password`, `share_expire_at` 字段

---

## 🎯 开发计划（按优先级）

### 📌 **任务1：历史对比分析** (预计45分钟)

#### 需要创建的文件/修改
1. **Schema** ([app/schemas/assessment.py](cci:7://file:///d:/%E6%A1%8C%E9%9D%A2/%E7%A5%9E%E6%80%9D%E6%9D%AF/backend/app/schemas/assessment.py:0:0-0:0))
   - `CompareAssessmentsRequest` - 对比请求参数
   - `ComparisonResponse` - 对比结果响应

2. **API端点** ([app/api/v1/assessment.py](cci:7://file:///d:/%E6%A1%8C%E9%9D%A2/%E7%A5%9E%E6%80%9D%E6%9D%AF/backend/app/api/v1/assessment.py:0:0-0:0))
   - `GET /assessment/compare` - 对比两次评估

#### 具体实现
```python
# 新增端点功能：
- 查询两条评估记录（验证属于同一用户）
- 计算风险分数差异、百分比变化
- 对比各分类风险（雷达图数据）
- 对比关键因素变化
- 生成变化总结文本
- 返回前端可用的对比数据结构
```

---

### 📌 **任务2：报告分享功能** (预计1-1.5小时)

#### 需要创建的文件/修改
1. **Schema** (`app/schemas/share.py` 新建)
   - `CreateShareRequest` - 创建分享请求
   - `ShareResponse` - 分享响应（含token、URL）
   - `AccessShareRequest` - 访问分享请求（可选密码）

2. **API端点** (新建 `app/api/v1/share.py`)
   - `POST /share/create` - 创建分享链接
   - `GET /share/{token}` - 访问分享报告（无需登录）
   - `DELETE /share/{token}` - 取消分享

3. **工具函数** ([app/utils/helpers.py](cci:7://file:///d:/%E6%A1%8C%E9%9D%A2/%E7%A5%9E%E6%80%9D%E6%9D%AF/backend/app/utils/helpers.py:0:0-0:0))
   - `generate_share_token()` - 生成随机token
   - `hash_password()` - 分享密码哈希（可选）

#### 具体实现
```python
# 功能流程：
1. 创建分享：
   - 生成唯一token（UUID或随机字符串）
   - 设置过期时间（7天/30天/永久）
   - 可选：加密分享密码
   - 更新Report表的share字段
   - 返回分享URL和二维码数据

2. 访问分享：
   - 验证token有效性
   - 检查是否过期
   - 验证密码（如有）
   - 返回脱敏后的报告数据（隐藏敏感信息）
   - 增加访问计数
```

---

### 📌 **任务3：管理员筛查记录管理** (预计45分钟)

#### 需要创建的文件/修改
1. **Schema** (`app/schemas/admin.py` 新建)
   - `AssessmentAdminListResponse` - 管理员视角的评估列表
   - `StatisticsDetailResponse` - 详细统计数据

2. **API端点** ([app/api/v1/admin.py](cci:7://file:///d:/%E6%A1%8C%E9%9D%A2/%E7%A5%9E%E6%80%9D%E6%9D%AF/backend/app/api/v1/admin.py:0:0-0:0) 扩展)
   - `GET /admin/assessments` - 所有筛查记录（分页、筛选）
   - `GET /admin/assessments/{id}` - 查看任意用户的评估详情
   - `GET /admin/statistics/detail` - 详细统计分析
   - `DELETE /admin/assessments/{id}` - 删除评估记录

#### 具体实现
```python
# 功能要点：
1. 筛查记录列表：
   - 分页查询所有用户的评估
   - 筛选：用户ID、风险等级、时间范围
   - 搜索：用户邮箱、昵称
   - 关联查询：显示用户基本信息

2. 详细统计：
   - 筛查趋势图数据（按日/周/月）
   - 风险等级分布（饼图）
   - 高危因素TOP10（柱状图）
   - 各类肿瘤风险分布
   - 平均模型推理时间
```

---

## 📝 详细实施步骤

### **第一步：创建Schema（15分钟）**

```python
# 1. app/schemas/assessment.py 新增
class CompareAssessmentsRequest(BaseModel):
    assessment_id_1: str
    assessment_id_2: str

class ComparisonResult(BaseModel):
    field: str
    value_1: Any
    value_2: Any
    change: Optional[float]
    change_percentage: Optional[float]

class ComparisonResponse(BaseModel):
    assessment_1: Dict[str, Any]
    assessment_2: Dict[str, Any]
    time_diff_days: int
    risk_score_change: float
    category_risks_comparison: List[Dict]
    key_factors_changes: List[Dict]
    summary: str

# 2. 新建 app/schemas/share.py
class CreateShareRequest(BaseModel):
    assessment_id: str
    expire_days: Optional[int] = Field(7, ge=1, le=365)
    password: Optional[str] = Field(None, min_length=4, max_length=20)

class ShareResponse(BaseModel):
    share_token: str
    share_url: str
    expire_at: Optional[datetime]
    qr_code_data: Optional[str]

# 3. 新建 app/schemas/admin.py
class AssessmentAdminSummary(BaseModel):
    id: str
    user_email: str
    user_nickname: Optional[str]
    risk_score: float
    risk_level: str
    created_at: datetime
```

### **第二步：实现对比分析API（30分钟）**

在 [app/api/v1/assessment.py](cci:7://file:///d:/%E6%A1%8C%E9%9D%A2/%E7%A5%9E%E6%80%9D%E6%9D%AF/backend/app/api/v1/assessment.py:0:0-0:0) 添加：

```python
@router.get("/compare", summary="对比两次评估")
async def compare_assessments(
    id1: str = Query(..., description="第一条评估ID"),
    id2: str = Query(..., description="第二条评估ID"),
    user_id: str = Depends(get_current_user_id),
    db: Session = Depends(get_db)
):
    # 实现逻辑...
```

### **第三步：实现分享功能（1小时）**

创建 `app/api/v1/share.py`：

```python
from fastapi import APIRouter, Depends, HTTPException
from app.models.assessment import Report
from app.utils.helpers import generate_share_token
import secrets
from datetime import datetime, timedelta

router = APIRouter()

@router.post("/create")
async def create_share_link(...):
    # 实现逻辑...

@router.get("/{token}")
async def access_shared_report(...):
    # 实现逻辑...
```

### **第四步：扩展管理员功能（45分钟）**

在 [app/api/v1/admin.py](cci:7://file:///d:/%E6%A1%8C%E9%9D%A2/%E7%A5%9E%E6%80%9D%E6%9D%AF/backend/app/api/v1/admin.py:0:0-0:0) 添加：

```python
@router.get("/assessments")
async def get_all_assessments(...):
    # 实现逻辑...

@router.get("/statistics/detail")
async def get_detailed_statistics(...):
    # 实现逻辑...
```

### **第五步：路由注册（5分钟）**

在 [app/main.py](cci:7://file:///d:/%E6%A1%8C%E9%9D%A2/%E7%A5%9E%E6%80%9D%E6%9D%AF/backend/app/main.py:0:0-0:0) 中添加分享路由：

```python
from app.api.v1 import auth, assessment, admin, share

app.include_router(
    share.router,
    prefix="/api/v1/share",
    tags=["分享"]
)
```

---

## ⏱️ 时间分配

| 任务 | 预计时间 | 复杂度 |
|------|---------|--------|
| 创建所有Schema | 15分钟 | ⭐ |
| 历史对比分析API | 30分钟 | ⭐⭐ |
| 报告分享功能 | 60-90分钟 | ⭐⭐⭐ |
| 管理员筛查记录 | 45分钟 | ⭐⭐ |
| 测试与调试 | 30分钟 | ⭐⭐ |
| **总计** | **3-3.5小时** | |

---

## 🚀 立即开始？

我建议按以下顺序实施：

1. **先做对比分析** - 最简单，快速见效
2. **再做管理员记录** - 利用现有admin代码
3. **最后做分享功能** - 最复杂，需要更多测试

**现在开始实现吗？** 我可以立即为您编写完整代码！请告诉我：
- 是按这个顺序开始吗？
- 还是您想调整优先级？
- 需要我先实现哪一个功能？